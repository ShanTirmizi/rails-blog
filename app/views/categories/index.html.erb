<h1>categories</h1>
<%= will_paginate @categories %>
<% @categories.each do |category|%>
  <div>
    <p> <%= link_to category.name, category_path(category) %> </p>

  </div>
<% end %>
<%= will_paginate @categories %>
